% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve-model.R
\name{solve_model}
\alias{solve_model}
\title{Solve a DSGE model from a .mod file}
\usage{
solve_model(
  mod_file,
  params = NULL,
  order = 1L,
  Sigma_e = NULL,
  steady_options = list(),
  perturbation_options = list(),
  stoch_simul = TRUE,
  irf_horizons = 40L,
  ramsey = FALSE,
  ramsey_order = 1L,
  ramsey_options = list(),
  mcp = FALSE,
  mcp_options = list(),
  ss_mcp = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{mod_file}{Path to a Dynare \code{.mod} file, or a pre-parsed
\code{dynhr_mod} object.}

\item{params}{Named numeric parameter vector.  Defaults to the
calibrated values from the \code{.mod} file.}

\item{order}{Perturbation order: 1 (default), 2, 3, 4, or 5.}

\item{Sigma_e}{(order >= 2) \eqn{n_{exo} \times n_{exo}} shock
covariance matrix.  \code{NULL} uses the model's \code{shocks} block.}

\item{steady_options}{List of options passed to \code{solve_steady()},
e.g. \code{list(y0 = ..., tol = 1e-10)}.}

\item{perturbation_options}{List of options passed to
\code{solve_perturbation()}, e.g. \code{list(h = 1e-4, sigma3 = NULL)}.}

\item{stoch_simul}{If \code{TRUE} (default), compute IRFs and
theoretical moments at the solution.  Set to \code{FALSE} to skip.}

\item{irf_horizons}{Number of IRF periods when \code{stoch_simul = TRUE}
(default 40).}

\item{ramsey}{If \code{TRUE}, also run the Ramsey optimal policy
workflow at the solved parameters.  Requires a
\code{planner_objective(...)} in the \code{.mod} file.}

\item{ramsey_order}{Perturbation order for the Ramsey solution
(1 or 2, default 1).}

\item{ramsey_options}{List of additional options passed to
\code{ramsey_policy()}, e.g. \code{list(n_periods = 400, burn_in = 100)}.}

\item{mcp}{Logical: if TRUE, parse MCP tags and compute MCP-constrained
IRFs via \code{\link{mcp_solve_path}}.  Default FALSE.}

\item{mcp_options}{List of additional arguments passed to \code{\link{mcp_solve_path}}
(e.g. \code{list(method = "sparse", max_iter = 100L)}).}

\item{ss_mcp}{Logical: if TRUE and the model has MCP tags, solve the steady
state with bound constraints via the semi-smooth MCP solver.
Default FALSE.}

\item{verbose}{Print progress messages.}
}
\value{
An object of class \code{"dynhr_solved"} containing:
  \describe{
    \item{\code{model}}{Parsed \code{dynhr_mod}}
    \item{\code{compiled}}{Compiled model}
    \item{\code{ss}}{Steady-state result (\code{dynhr_steady})}
    \item{\code{dr}}{Decision rules object (\code{DecisionRules},
      \code{DecisionRules2}, etc.)}
    \item{\code{params}}{Parameter vector used}
    \item{\code{irfs}}{IRF list (if \code{stoch_simul = TRUE})}
    \item{\code{moments}}{Theoretical moments (if \code{stoch_simul = TRUE})}
    \item{\code{ramsey}}{Ramsey result (if \code{ramsey = TRUE})}
    \item{\code{meta}}{Run metadata}
  }
}
\description{
Parses, compiles, finds steady state, and solves the perturbation of a
DSGE model in a single call.  This is the recommended entry point for
all downstream analysis (mode-finding, estimation, diagnostics).
}
\examples{
\dontrun{
mod <- solve_model("my_model.mod")
mod <- solve_model("my_model.mod", order = 2)
mod <- solve_model("my_model.mod", ramsey = TRUE)
}
}
\seealso{
\code{\link{run_mode_finding}}, \code{\link{run_posterior_estimation}},
  \code{run_all_diagnostics}
}
