% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-panel.R
\name{.hank_hh_obs_weight}
\alias{.hank_hh_obs_weight}
\title{Gaussian observation weights for one period of a household HMM}
\usage{
.hank_hh_obs_weight(y_t, obs_spec, me_sd, a_level, c_pol_vec)
}
\arguments{
\item{y_t}{Named numeric vector (subset of \code{names(obs_spec)}, possibly
with \code{NA} entries for missing variables this period).}

\item{obs_spec}{Character vector of observed variable names, subset of
\code{c("a", "c")}.}

\item{me_sd}{Named numeric vector of measurement-error STANDARD DEVIATIONS,
keyed by the entries of \code{obs_spec}.}

\item{a_level}{Length-\code{n_cell} vector: the asset GRID level at each
cell (only needed if \code{"a" \%in\% obs_spec}).}

\item{c_pol_vec}{Length-\code{n_cell} vector: the period's consumption
policy at each cell, distribution-order (only needed if
\code{"c" \%in\% obs_spec}).}
}
\value{
Length-\code{n_cell} numeric vector of observation weights
  (product of per-variable Gaussian densities; uniform 1 where all
  observed variables are \code{NA} this period).
}
\description{
Gaussian observation weights for one period of a household HMM
}
\keyword{internal}
