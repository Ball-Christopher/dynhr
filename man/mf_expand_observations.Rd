% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-frequency.R
\name{mf_expand_observations}
\alias{mf_expand_observations}
\title{Place low-frequency observations on a high-frequency grid}
\usage{
mf_expand_observations(x, k, n_T = NULL, offset = 0L)
}
\arguments{
\item{x}{Numeric vector of low-frequency observations (\code{NA} allowed).}

\item{k}{Aggregation length (high-frequency periods per observation).}

\item{n_T}{Length of the high-frequency sample. Default: just long enough
to hold every element of \code{x}.}

\item{offset}{Number of leading high-frequency periods before the first
aggregation window closes (default \code{0}).}
}
\value{
Numeric vector of length \code{n_T}, \code{NA} except at the
  aggregation dates.
}
\description{
Convenience helper for building the \code{NA}-padded column an aggregated
observable needs: the \code{j}-th low-frequency value is written at
high-frequency period \code{offset + j * k}, every other period is
\code{NA}.  With the default \code{offset = 0} the first observation sits
at period \code{k}, i.e. at the END of the first complete aggregation
window -- which is what the weights in
\code{\link{mf_aggregation_weights}} assume.
}
\examples{
mf_expand_observations(c(1.2, 0.8), k = 3)
}
\seealso{
\code{\link{mf_aggregation_weights}}
}
