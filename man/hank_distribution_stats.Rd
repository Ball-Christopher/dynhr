% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-diagnostics.R
\name{hank_distribution_stats}
\alias{hank_distribution_stats}
\title{Wealth/consumption distribution statistics for a het block}
\usage{
hank_distribution_stats(
  block,
  top = c(0.1, 0.01),
  probs = c(0.5, 0.9, 0.99),
  constraint_tol = 1e-09
)
}
\arguments{
\item{block}{A \code{\link{hank_het_block}}.}

\item{top}{Numeric vector of top-share fractions (default \code{c(0.1, 0.01)}
for the top-10\% and top-1\% wealth shares).}

\item{probs}{Numeric vector of percentile levels for the wealth quantiles.}

\item{constraint_tol}{Agents with assets within this of \code{amin} are
counted as hand-to-mouth.}
}
\value{
A list with \code{gini} (wealth), \code{top_shares} (named by
  \code{top}), \code{hand_to_mouth} (mass fraction at the constraint),
  \code{mean_wealth}, \code{quantiles}, and \code{gini_consumption}.
}
\description{
Summarizes the stationary cross-sectional distribution: Gini, top wealth
shares, the hand-to-mouth fraction (mass at the borrowing constraint), and
wealth percentiles.
}
\examples{
inc <- hank_income_rouwenhorst(0.9, 0.7, 3)
ag  <- hank_asset_grid(100, 80, 0)
blk <- hank_het_block(ag, inc$Pi, inc$e, beta = 0.96, eis = 1, r = 0.02, w = 1)
hank_distribution_stats(blk)$gini
}
