% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/system-priors.R
\name{sp_irf}
\alias{sp_irf}
\title{System prior on an impulse response}
\usage{
sp_irf(var, shock, horizon, density, n_periods = NULL)
}
\arguments{
\item{var}{Endogenous variable name (character).}

\item{shock}{Exogenous shock name (character).}

\item{horizon}{Response horizon (integer >= 1).}

\item{density}{Density specification (see \code{.sp_entry}).
Common choices:
\itemize{
  \item \code{list(type = "hard_sign", sign = "positive")} -- IRF > 0.
  \item \code{list(dist = "normal", p1 = 0.5, p2 = 0.2)} -- soft N(0.5,
    0.2²) prior on the IRF coefficient.
}}

\item{n_periods}{Total IRF periods to compute (must be >= \code{horizon};
default \code{horizon}).}
}
\value{
A list of class \code{"sp_entry"}.
}
\description{
Creates a system-prior entry based on the impulse response of variable
\code{var} to shock \code{shock} at horizon \code{horizon}.  The feature
value is the IRF coefficient at that horizon (in model units, i.e. deviation
from steady state per one standard-deviation shock).
}
\details{
The computation reuses \code{compute_irfs(dr, model, n_periods, params)}
from \code{R/stochsimul-monolith.R} — no re-solve.
}
\seealso{
\code{\link{system_prior_spec}}, \code{\link{sp_variance_share}}
}
