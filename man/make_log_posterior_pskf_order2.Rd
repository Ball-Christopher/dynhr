% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pskf-likelihood.R
\name{make_log_posterior_pskf_order2}
\alias{make_log_posterior_pskf_order2}
\title{Create a PSKF log-posterior evaluator on the pruned ORDER-2 state space}
\usage{
make_log_posterior_pskf_order2(
  model,
  data,
  prior_spec,
  obs_vars,
  compiled,
  me_variance = 0,
  system_priors = NULL,
  cut_tol = 0.01,
  max_q = 5L,
  ...
)
}
\arguments{
\item{model}{dynhr_mod (from \code{parse_mod}).}

\item{data}{Observation matrix (T x n_obs or n_obs x T).}

\item{prior_spec}{Prior specification (from \code{extract_prior_spec}).}

\item{obs_vars}{Character vector of observed variable names.}

\item{compiled}{dynhr_compiled (from \code{compile_model}, order >= 2).}

\item{me_variance}{Measurement-error variance (default 0).}

\item{system_priors}{Named list of system-prior closures, or NULL.}

\item{cut_tol}{Pruning tolerance (default 0.01; see \code{.pskf_filter}).}

\item{max_q}{Hard cap on the retained skew dimension (default 5, the
Miwa-exact Phi_q range).}

\item{...}{Ignored (interface compatibility).}
}
\value{
function(theta) -> list(logpost, loglik, logprior)
}
\description{
Factory (mirror of \code{make_log_posterior_pskf}) that runs the
closed-skew-normal (CSN) Kalman filter on the AFVRR (2018) pruned
second-order augmented state space, so skewed shocks propagate through the
second-order dynamics.  The augmented system is linear in the augmented
state, so the SAME CSN machinery (\code{.pskf_filter} + the shock-CSN lift
\code{.csn_state_noise_lift}) applies once the raw innovation \eqn{r_t} is
carried in the state (decorrelating the state/observation noise; see the
derivation note in this file).
}
\details{
At \code{alpha = 0} for every shock this reduces EXACTLY (to \eqn{O(10^{-13})})
to the Gaussian pruned-order-2 filter \code{\link{pruned_ss_loglik}}.
}
