% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-aggregate-risk.R
\name{.hank_agg_backward}
\alias{.hank_agg_backward}
\title{Backward EGM sweep on the (a, e) x (K, Z) state space}
\usage{
.hank_agg_backward(
  a_grid,
  Pi,
  e,
  beta,
  eis,
  alpha,
  delta,
  Z_grid,
  Pi_Z,
  K_grid,
  rule_a,
  rule_b,
  amin = NULL,
  Va_init = NULL,
  tol = 1e-10,
  maxit = 5000L
)
}
\arguments{
\item{a_grid, Pi, e, beta, eis}{Household calibration (as
\code{\link{hank_het_block}}).}

\item{alpha, delta}{Firm calibration.}

\item{Z_grid, Pi_Z}{Aggregate chain.}

\item{K_grid}{Aggregate capital grid (increasing, positive).}

\item{rule_a, rule_b}{Length-\code{n_z} forecast-rule coefficients,
\eqn{\log K' = a_z + b_z \log K}.}

\item{amin}{Borrowing constraint.}

\item{Va_init}{Optional \code{(n_e*n_a) x (n_k*n_z)} warm start.}

\item{tol, maxit}{Convergence tolerance on the savings policy, iteration cap.}
}
\value{
A list with \code{Va}, \code{a_pol}, \code{c_pol} (each
  \code{(n_e*n_a) x (n_k*n_z)}, column-major within the \code{n_e x n_a}
  block), \code{r_node}, \code{w_node} (\code{n_k x n_z}),
  \code{iterations}, \code{converged}, \code{gap}.
}
\description{
Iterates the SAME one-asset EGM kernel used by \code{\link{hank_egm_solve}}
(\code{\link{.hank_egm_step}}) at every aggregate node \code{(K_k, Z_z)},
with the continuation value supplied by
\code{\link{.hank_agg_expectation_operator}}.  Because idiosyncratic
\eqn{e'} and aggregate \eqn{Z'} are independent, the double expectation
factors: the \eqn{Z'} average is taken first (by the operator) and
\code{.hank_egm_step}'s own \code{Pi \%*\% Va_p} then takes the \eqn{e'}
average, so the kernel is reused verbatim rather than reimplemented.
}
\keyword{internal}
