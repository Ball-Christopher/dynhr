% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-finite-ramsey-ss.R
\name{.hank_taxed_het_block}
\alias{.hank_taxed_het_block}
\title{Taxed household block at fixed (tau, r, w)}
\usage{
.hank_taxed_het_block(
  a_grid,
  Pi,
  e,
  beta,
  eis,
  tau,
  r,
  w,
  K,
  tol = 1e-11,
  maxit = 5000L
)
}
\arguments{
\item{a_grid, Pi, e}{Household grid, income transition, income levels.}

\item{beta, eis}{Discount factor and EIS.}

\item{tau}{Capital-income tax rate.}

\item{r, w}{Pre-tax return and wage.}

\item{K}{Aggregate capital (needed only to compute the lump-sum rebate
\code{tr = tau*r*K}; pass the candidate market-clearing \code{K(r)}).}

\item{tol, maxit}{Passed to \code{\link{hank_egm_solve}}.}
}
\value{
A list with the same shape as \code{\link{hank_het_block}}
  (\code{a}, \code{c}, \code{Va}, \code{Lambda}, \code{D}, \code{A},
  \code{C}), plus \code{tau}, \code{tr}, \code{r} (pre-tax), \code{r_net}.
}
\description{
Solves the household EGM problem under a linear capital tax \code{tau} with
balanced-budget lump-sum rebate \code{tr = tau*r*K}, using the KEY
EQUIVALENCE documented in the file header: this is EXACTLY
\code{\link{hank_egm_solve}} with net return \code{r_net = (1-tau)*r} and
income \code{y = w*e + tr}. Mirrors \code{\link{hank_het_block}}'s
packaging (policies, distribution, aggregates) but for the taxed problem;
does not modify \code{hank_het_block} itself.
}
\keyword{internal}
