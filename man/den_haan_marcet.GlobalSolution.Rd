% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global-solution-methods.R
\name{den_haan_marcet.GlobalSolution}
\alias{den_haan_marcet.GlobalSolution}
\title{Den Haan-Marcet statistic for a GlobalSolution}
\usage{
\method{den_haan_marcet}{GlobalSolution}(
  x,
  n_sim = 10000L,
  burn_in = 200L,
  instruments = NULL,
  normalize_by = "auto",
  equations = NULL,
  seed = NULL,
  init_state = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{GlobalSolution}.}

\item{n_sim}{Simulation length AFTER burn-in (default 10000).}

\item{burn_in}{Discarded initial periods (default 200).}

\item{instruments}{Optional \code{n_sim x q} instrument matrix.
\code{NULL} (default) uses \code{(1, state lags in deviation)}.}

\item{normalize_by}{Numeraire variable; see \code{\link{euler_errors}}.}

\item{equations}{Integer positions of the equations to test.  \code{NULL}
(default) uses the EXPECTATIONAL equations (those carrying a \eqn{t+1}
term); a static identity has an identically-zero residual and would make
the weighting matrix singular.}

\item{seed}{Optional integer seed.  Applied through
\code{.with_local_seed()}, so the caller's RNG stream is untouched.}

\item{init_state}{Optional named initial state lag (default: steady state).}

\item{...}{Ignored.}
}
\value{
A list with \code{J_stat}, \code{df}, \code{p_value},
  \code{crit_95}, \code{reject}, \code{n_used}, \code{equations},
  \code{numeraire} and the per-equation \code{residual_sd}.
}
\description{
Simulates the projection policy, forms the realised one-step model
residuals in the same (consumption-equivalent where available) units as
\code{\link{euler_errors}}, and tests their orthogonality to a set of
time-\eqn{t} instruments.  Under the null that the policy solves the model
exactly the statistic is \eqn{\chi^2} with \code{df} degrees of freedom, so
\code{J_stat > crit_95} rejects the policy.
}
