% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global-likelihood.R
\name{make_log_posterior_global_pf}
\alias{make_log_posterior_global_pf}
\title{Log-posterior with a global-solution bootstrap particle filter}
\usage{
make_log_posterior_global_pf(
  model,
  data,
  prior_spec,
  obs_vars,
  compiled,
  me_variance = 0,
  n_particles = 1000L,
  poly_degree = 3L,
  n_quad = 5L,
  n_nodes = 7L,
  state_domain = NULL,
  solve_tol = 1e-07,
  solve_max_iter = 200L,
  ess_frac = 0.5,
  seed = NULL,
  power = NULL,
  system_priors = NULL
)
}
\arguments{
\item{model}{Parsed model from \code{\link{parse_mod}}.}

\item{data}{Observation matrix (\code{n_obs x T} or \code{T x n_obs};
re-oriented automatically, as in \code{\link{kalman_filter}}).}

\item{prior_spec}{Prior specification from \code{\link{extract_prior_spec}}.}

\item{obs_vars}{Character vector of observed variable names.}

\item{compiled}{\code{dynhr_compiled} from \code{\link{compile_model}}.}

\item{me_variance}{Measurement-error variance, a positive scalar.
\strong{Required}: a bootstrap filter cannot weight particles without it.}

\item{n_particles}{Number of particles (default 1000).}

\item{poly_degree, n_quad, n_nodes, state_domain, solve_tol, solve_max_iter}{Passed to \code{\link{solve_global}} at every draw
(\code{solve_tol}/\code{solve_max_iter} are its \code{tol}/\code{max_iter}).}

\item{ess_frac}{Resample threshold as a fraction of \code{n_particles}
(default 0.5); see Details in \code{\link{make_log_posterior_tpf}} for the
same convention.}

\item{seed}{Integer seed, or \code{NULL} (default) for fresh
randomness at every evaluation.  A non-\code{NULL} seed is applied
through \code{.with_local_seed()}, so the caller's global RNG stream is
left exactly as it was found.}

\item{power}{Power-posterior tempering exponent zeta;
\code{NULL} (default) resolves the \code{power_posterior} option once at
factory time, as in \code{\link{make_log_posterior}}.}

\item{system_priors}{Optional system-prior specification.}
}
\value{
A \code{function(theta)} returning
  \code{list(logpost, loglik, logprior)}.
}
\description{
Solves the model GLOBALLY (Chebyshev projection, \code{\link{solve_global}})
at every parameter draw and evaluates the likelihood with a bootstrap
particle filter run on the resulting NONLINEAR policy: the state transition
is the policy plus the drawn shocks and the measurement density is
\eqn{N(obs_t; y_t[obs\_vars], \sigma^2_{me} I)}.  Unlike every other
likelihood in the package this one never linearises the model, so it is the
path to take when the nonlinearity IS the object of interest.
}
\details{
The returned estimate is unbiased for the marginal likelihood, so
\code{\link{pmmh}()} -- random-walk Metropolis driven by an unbiased
likelihood estimate -- is valid over this closure, provided
\code{seed = NULL} (the default).
}
\seealso{
\code{\link{solve_global}}, \code{\link{make_log_posterior}},
  \code{\link{euler_errors}}
}
