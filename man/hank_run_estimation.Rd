% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-run-ar.R
\name{hank_run_estimation}
\alias{hank_run_estimation}
\title{Posterior sampling for a HANK model under the exact-AR likelihood}
\usage{
hank_run_estimation(
  mode_result,
  method = c("RWMH", "NUTS"),
  n_draws = 2000L,
  n_burn = 1000L,
  Sigma_prop = NULL,
  transform_params = TRUE,
  seed = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{mode_result}{A \code{hank_ar_mode} from
\code{\link{hank_run_mode_finding}}.}

\item{method}{\code{"RWMH"} (default) or \code{"NUTS"}. NUTS uses the exact
score (\code{target$grad_fn}); that is the case the whole exact-AR
gradient stack exists for.}

\item{n_draws, n_burn}{Sampler lengths (NUTS reads \code{n_burn} as warmup).}

\item{Sigma_prop}{RWMH proposal covariance; default a diagonal built from
the prior sds, scaled by \code{0.1^2}.}

\item{transform_params}{Sample in unconstrained eta-space (default
\code{TRUE}); the change-of-variables Jacobian IS included here, unlike in
mode-finding.}

\item{seed}{Optional RNG seed.}

\item{verbose}{Print progress.}

\item{...}{Passed to the sampler.}
}
\value{
The sampler's own result object, with \code{target} and
  \code{theta_mode} attached.
}
\description{
The \code{likelihood = "exact_ar"} counterpart of
\code{\link{run_full_estimation}} / \code{\link{run_posterior_estimation}}:
runs a sampler on the mode result from
\code{\link{hank_run_mode_finding}}, reusing that run's target -- and
therefore its model memo and likelihood cache -- rather than rebuilding a
posterior.
}
\seealso{
\code{\link{hank_run_mode_finding}}, \code{\link{hank_ar_target}}
}
