% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kf-step.R
\name{kf_step}
\alias{kf_step}
\title{One warm-started Kalman filter step}
\usage{
kf_step(
  s,
  P,
  y,
  TT,
  ZZ,
  RR,
  DD,
  Sigma_e,
  scale = NULL,
  d = NULL,
  me_diag = NULL,
  me_extra = NULL
)
}
\arguments{
\item{s}{Prediction state mean \eqn{E[x_t \mid y_{1:t-1}]}, length n_s.}

\item{P}{Prediction state covariance, n_s x n_s.}

\item{y}{Observation at period t, length n_obs.}

\item{TT}{State transition, n_s x n_s.}

\item{ZZ}{State-to-observation loading, n_obs x n_s.}

\item{RR}{Shock-to-state impact, n_s x n_e.}

\item{DD}{Shock-to-observation impact, n_obs x n_e.}

\item{Sigma_e}{Baseline shock covariance, n_e x n_e.}

\item{scale}{Optional length-n_e multiplicative factors on shock standard
deviation for THIS period (\code{exp(h_t/2)} in the SV filter). \code{NULL}
(default) = baseline (all ones).}

\item{d}{Optional observation intercept (SS level of the observables),
length n_obs. \code{NULL} = zero.}

\item{me_diag}{Optional length-n_obs baseline measurement-error variances.
TRUE i.i.d. observation noise (F4-A): added to the diagonal of \code{F}
AND propagated through the Joseph state-covariance term
(\code{P' += K diag(me) K'}), matching \code{\link{kalman_filter}}.}

\item{me_extra}{Optional length-n_obs per-period EXTRA measurement-error
variances (same true-noise treatment, added on top of \code{me_diag}).
\code{NULL} = none.}
}
\value{
\code{list(ll, s, P)} — the period log-likelihood increment and the
  next-period prediction mean/covariance — or \code{NULL} if the forecast
  covariance is non-finite or not positive definite, so a caller can treat
  the draw as infeasible. (Both rejections match the compiled RB-PF kernel
  exactly, so the R and C++ filters weight the same particles.)
}
\description{
Advances the prediction-form Kalman recursion one period, optionally with a
per-period multiplicative shock-standard-deviation \code{scale} (the SV /
heteroskedastic input) and per-period extra measurement-error variances.
This is the exact single-period map used inside the SV Rao-Blackwellized
particle filter (\code{\link{make_log_posterior_sv_rbpf}}).
}
\seealso{
\code{\link{kf_stationary_init}}, \code{\link{make_log_posterior_sv_rbpf}}
}
