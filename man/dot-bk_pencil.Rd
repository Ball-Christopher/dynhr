% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bk-distance.R
\name{.bk_pencil}
\alias{.bk_pencil}
\title{Build the reduced companion-form Blanchard-Kahn pencil at a parameter point}
\usage{
.bk_pencil(model, compiled, params = NULL, ss = NULL)
}
\arguments{
\item{model, compiled}{A parsed \code{dynhr_mod} and its
\code{\link{compile_model}} output.}

\item{params}{Named parameter vector (defaults to \code{model$param_values}).}

\item{ss}{Optional precomputed steady state (\code{$values} or a bare
numeric vector). When \code{NULL}, \code{\link{solve_steady}} is called.}
}
\value{
A list with \code{D}, \code{E} (the \eqn{p \times p} pencil),
  \code{n_minus} (predetermined-block size) and \code{p}; or \code{NULL} if
  the model has no dynamic (forward/mixed) block.
}
\description{
Thin wrapper that drives \code{solve_perturbation}'s order-1 machinery only
as far as the reduced companion pencil \eqn{(D, E)} (no QZ decomposition),
so pencil derivatives can be finite-differenced without paying for -- or
depending on the eigenvalue ordering of -- a full solve.
}
\keyword{internal}
