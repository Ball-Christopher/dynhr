% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-het-block.R
\name{.hank_na_real}
\alias{.hank_na_real}
\title{A missing run-metadata scalar, as NA_real_}
\usage{
.hank_na_real(v)
}
\arguments{
\item{v}{The value read off the solve, possibly \code{NULL}.}
}
\value{
\code{v} when it is a length-1 numeric, \code{NA_real_} otherwise.
}
\description{
Solve routines that predate a metadata field return \code{NULL} for it.
Recording \code{NULL} would DROP the field from the block's list (and so
silently shift every later field), while recording a made-up number would
be worse still, so the block stores \code{NA_real_}: present, and honest
about being unknown.
}
\keyword{internal}
