% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag-posterior-predictive.R
\name{diag_posterior_predictive}
\alias{diag_posterior_predictive}
\title{Posterior predictive checks for a DSGE model}
\usage{
diag_posterior_predictive(
  chains,
  model_solve_fn,
  data_moments,
  moment_names = NULL,
  n_ppc = 500L,
  ppp_warn_lo = 0.05,
  ppp_warn_hi = 0.95,
  meta = NULL
)
}
\arguments{
\item{chains}{\code{dynhr_chains} object or draw matrix
(n_draws x n_params) from the estimated posterior.}

\item{model_solve_fn}{Function \code{theta -> named numeric} returning
model-implied moments (same interface as used by D4).}

\item{data_moments}{Named numeric vector of empirical sample moments.}

\item{moment_names}{Character vector (optional; inferred from
\code{data_moments}).}

\item{n_ppc}{Number of posterior draws to use for PPC
(default 500; capped at number of available draws).}

\item{ppp_warn_lo}{Lower PPP threshold for flagging (default 0.05).}

\item{ppp_warn_hi}{Upper PPP threshold for flagging (default 0.95).}

\item{meta}{Optional \code{\link{diag_meta}} provenance object.}
}
\value{
A \code{dynhr_diagnostic} object with \code{$result$ppp_values},
  \code{$result$post_moments}, \code{$plots}, \code{$summary}, and
  \code{$llm_summary}.
}
\description{
Draws \code{n_draws} parameter vectors uniformly from the posterior chains
(with replacement), solves the model at each draw, collects model-implied
moments, and computes posterior-predictive p-values (PPP) by comparing
the posterior-predictive distribution of each moment to the corresponding
empirical data value.
}
\details{
A PPP near 0 means the model systematically under-produces that moment
relative to the data; near 1 means it over-produces.  PPP outside
\code{[ppp_warn_lo, ppp_warn_hi]} are flagged.
}
