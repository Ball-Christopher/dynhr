% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-egm.R
\name{hank_egm_solve}
\alias{hank_egm_solve}
\title{Solve the household problem to a steady-state policy (fixed prices)}
\usage{
hank_egm_solve(
  a_grid,
  y,
  r,
  beta,
  eis,
  Pi,
  tol = 1e-11,
  maxit = 5000L,
  Va_init = NULL,
  amin = NULL
)
}
\arguments{
\item{a_grid}{Numeric length-\code{n_a}: fixed asset grid (increasing).}

\item{y}{Numeric length-\code{n_e}: income by state this period.}

\item{r}{Numeric: real return on assets this period.}

\item{beta}{Numeric: discount factor.}

\item{eis}{Numeric: elasticity of intertemporal substitution (\eqn{1/\gamma}).}

\item{Pi}{Numeric \code{n_e x n_e}: income transition matrix (row-stochastic).}

\item{tol}{Numeric: convergence tolerance on the max absolute change in the
savings policy between backward iterations.}

\item{maxit}{Integer: maximum backward iterations.}

\item{Va_init}{Optional \code{n_e x n_a} initial marginal value; if
\code{NULL}, a standard consume-10\%-of-cash-on-hand guess is used.}

\item{amin}{Numeric: borrowing constraint (minimum end-of-period assets).
Must satisfy \code{amin >= a_grid[1]}. Defaults to \code{NULL}, which
resolves to \code{a_grid[1L]} (the historical hardcoded behavior, so
existing calls are byte-identical). A tighter (higher) \code{amin}
decouples the borrowing limit from the grid floor, letting different
household types face different constraints on a shared grid.}
}
\value{
A list with converged \code{Va}, \code{a} (savings policy), \code{c}
  (consumption policy), plus \code{iterations} and \code{converged}. The
  resolved \code{amin} is carried on the return list.
}
\description{
Iterates the EGM backward step at constant prices \code{(r, y)} until the
savings policy converges.
}
\examples{
inc <- hank_income_rouwenhorst(0.9, 0.7, 5)
a   <- hank_asset_grid(50, 100, 0)
hh  <- hank_egm_solve(a, y = inc$e, r = 0.01, beta = 0.96,
                      eis = 1, Pi = inc$Pi)
hh$converged
}
