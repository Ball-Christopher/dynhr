% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-aggregate-risk.R
\name{.hank_agg_default_K_grid}
\alias{.hank_agg_default_K_grid}
\title{Default aggregate-capital grid, sized off the LINEAR model}
\usage{
.hank_agg_default_K_grid(
  ks,
  sigma_eps,
  rho,
  n_k = 7L,
  n_sd = 4,
  min_span = 0.01,
  T_h = 200L
)
}
\arguments{
\item{ks}{A \code{\link{hank_ks_steady}} steady state.}

\item{sigma_eps}{Innovation sd of \eqn{\log Z} (0 for a degenerate chain).}

\item{rho}{Persistence of \eqn{\log Z}.}

\item{n_k}{Integer >= 2: number of aggregate-capital nodes.}

\item{n_sd}{Numeric: half-width in stationary sd of \eqn{\log K}.}

\item{min_span}{Numeric: floor on the half-width in log points.}

\item{T_h}{Integer: horizon used for the MA truncation.}
}
\value{
A list with \code{K_grid} (increasing, log-spaced) and
  \code{sd_logK} (the linear-model stationary sd used to size it).
}
\description{
The ergodic spread of \eqn{\log K} is not known before the model is solved,
so the default grid is sized from the certainty-equivalent (sequence-space)
solution, which is available for free: feed the AR(1) TFP impulse into
\code{\link{hank_ks_linear_irf}} to get the MA coefficients
\eqn{\psi_h = d\log K_{h}/d\varepsilon} and read off
\eqn{sd(\log K) = \sigma_\varepsilon \sqrt{\sum_h \psi_h^2}}.
The grid then spans \code{n_sd} stationary standard deviations either side
of \eqn{\log K_{ss}}, with a floor (\code{min_span}) so that a degenerate
or near-degenerate chain still gets a non-trivial, well-conditioned grid.
}
\keyword{internal}
