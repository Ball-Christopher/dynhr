% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pruned-grad-chain-order3.R
\name{.pgo3_grad_chain}
\alias{.pgo3_grad_chain}
\title{End-to-end semi-analytic d(order-3 pruned loglik)/d(theta) for ALL params}
\usage{
.pgo3_grad_chain(
  theta,
  model,
  compiled,
  par_names,
  Y,
  obs_vars,
  me_variance = 0
)
}
\arguments{
\item{theta}{Named numeric parameter vector.}

\item{model, compiled}{Model + compiled objects.}

\item{par_names}{Character vector of parameter names to differentiate.}

\item{Y}{n_obs x T data matrix.}

\item{obs_vars}{Character vector of observed endogenous variable names.}

\item{me_variance}{Scalar measurement-error variance (default 0).}
}
\value{
\code{NULL} if the base assembly fails (caller falls back to prior-
  only / FD-of-forward for everything), else a list with \code{loglik}
  (scalar) and \code{grad} (named numeric vector, one entry per
  \code{par_names}; \code{NA} for any parameter whose assembly FD failed
  in either direction).
}
\description{
Runs ONE \code{.pruned_kf_correlated_adjoint()} pass at the base theta to
get \code{loglik} and the nine input-gradients \code{bar_M}, then for each
parameter takes central FD of the ASSEMBLY ONLY (\code{.pgo3_assembly_fd},
no filter pass) and contracts: \code{sum(bar_M * dM/dtheta_j)} summed over
the nine inputs.
}
\keyword{internal}
