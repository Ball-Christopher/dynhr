% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obc-ppf-importance.R
\name{ppf_reweight_posterior}
\alias{ppf_reweight_posterior}
\title{PPF posterior importance re-weighting}
\usage{
ppf_reweight_posterior(
  chains,
  model,
  compiled,
  data,
  prior_spec,
  obs_vars,
  specs = NULL,
  n_thin = 10L,
  n_particles = 2000L,
  me_variance = 1e-04,
  seed = 42L
)
}
\arguments{
\item{chains}{Matrix of posterior draws (n_draws x n_params) or a
named list with \code{$chains} or \code{$mode} component. If a
single-row matrix, treated as the mode.}

\item{model}{dynhr_mod}

\item{compiled}{dynhr_compiled}

\item{data}{Observation matrix (T x n_obs or n_obs x T)}

\item{prior_spec}{Prior specification data.frame}

\item{obs_vars}{Character vector of observed variable names}

\item{specs}{OBC spec list (from obc_parse_tags), or NULL to parse}

\item{n_thin}{Thinning factor -- use every n_thin-th draw (default 10)}

\item{n_particles}{Number of PPF particles per draw (default 2000)}

\item{me_variance}{Measurement error variance (must be > 0; default 1e-4)}

\item{seed}{Integer RNG seed (default 42L)}
}
\value{
Named list with:
  \item{log_weights}{length-n vector of log importance weights}
  \item{weights}{length-n vector of normalized importance weights}
  \item{ess}{effective sample size}
  \item{ess_fraction}{ESS / n (near 1 = PKF adequate; < 0.3 = PKF biased)}
  \item{loglik_pkf}{length-n vector of re-evaluated PKF log-likelihoods}
  \item{loglik_ppf}{length-n vector of PPF log-likelihoods}
  \item{n_draws}{number of thinned draws used}
  \item{verdict}{character string diagnostic message}
}
\description{
Given existing PKF posterior draws (chains or a mode result), re-weights
them using PPF log-likelihoods. The importance weights are:
}
\details{
w_i = exp(loglik_PPF(theta_i) - loglik_PKF(theta_i))

Normalised in log-space to avoid overflow. ESS / n near 1.0 indicates the
PKF approximation is adequate; low ESS/n signals PKF-induced posterior bias.

PKF log-likelihoods are re-evaluated (chains do not carry per-draw logliks).
}
