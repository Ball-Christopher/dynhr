% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chain-state.R
\name{mcmc_chain_extend}
\alias{mcmc_chain_extend}
\title{Append new draws to a saved chain .rds}
\usage{
mcmc_chain_extend(
  file,
  new_draws,
  new_lp = NULL,
  draws_field = "z",
  lp_field = "lp",
  sweep_field = "sweep",
  done = TRUE
)
}
\arguments{
\item{file}{Path to the existing chain .rds.}

\item{new_draws}{Matrix (or vector for one sweep) of new draws;
column count must match the stored draw matrix.}

\item{new_lp}{Optional numeric vector of log-posterior values,
one per new sweep, appended to \code{lp_field}.}

\item{draws_field}{Name of the draw-matrix field (default \code{"z"}).}

\item{lp_field}{Name of the log-posterior field (default \code{"lp"}).}

\item{sweep_field}{Name of the sweep-counter field (default
\code{"sweep"}); re-stamped to the new total row count when present
in the list.}

\item{done}{Value to stamp into the list's \code{done} field
(default \code{TRUE}); set \code{NULL} to leave it untouched.}
}
\value{
The updated list, invisibly (the file is rewritten in place).
}
\description{
Concatenation helper for downstream diagnostics that glob per-chain
\code{*.rds} files: appends freshly drawn sweeps to the draw matrix in
an existing checkpoint list, re-stamps its sweep counter / done flag,
and rewrites the file atomically. The file must hold a plain
\code{list()} with the draw matrix under \code{draws_field}; all other
fields are preserved untouched.
}
