% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obc-ppf.R
\name{make_log_posterior_obc_ppf}
\alias{make_log_posterior_obc_ppf}
\title{Create a PPF-based log-posterior evaluator for OBC models}
\usage{
make_log_posterior_obc_ppf(
  model,
  data,
  prior_spec,
  obs_vars,
  compiled,
  specs = NULL,
  me_variance = 1e-04,
  N = 1000L,
  proposal = c("bootstrap", "copf"),
  regime_guess = c("ancestor", "slack"),
  seed = NULL
)
}
\arguments{
\item{model}{dynhr_mod}

\item{data}{Observation matrix (T x n_obs or n_obs x T)}

\item{prior_spec}{Prior specification data.frame}

\item{obs_vars}{Character vector of observed variable names}

\item{compiled}{dynhr_compiled}

\item{specs}{OBC spec list (from obc_parse_tags), or NULL to parse}

\item{me_variance}{Measurement error variance (must be > 0; default 1e-4)}

\item{N}{Number of particles per evaluation (default 1000)}

\item{proposal}{"bootstrap" (prior proposal) or "copf" (conditionally
optimal proposal with regime verification and general-ratio fallback)}

\item{regime_guess}{For proposal = "copf": "ancestor" (default) guesses
each particle's regime from its resampled ancestor's verified regime
(all-slack at t = 1); "slack" always guesses all-slack. Pure variance
reduction — weights are valid for any guess.}

\item{seed}{Integer RNG seed (NULL = not fixed; each call differs)}
}
\value{
Function(theta) -> list(logpost, loglik, logprior)
}
\description{
Bootstrap particle filter variant of \code{make_log_posterior_obc_pkf}.
Each evaluation builds a FRESH regime_cache (theta-dependent) and runs
the bootstrap PPF with N particles.
}
\details{
me_variance > 0 is required (hard stop): the bootstrap weights degenerate
when me_variance = 0 with n_obs < n_exo.
}
