% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-twoasset-model.R
\name{hank_het2_block_spec}
\alias{hank_het2_block_spec}
\title{Wrap a two-asset household as a sequence-space block}
\usage{
hank_het2_block_spec(
  name,
  block,
  inputs = c("rb", "ra", "w"),
  outputs = c("B", "A", "C")
)
}
\arguments{
\item{name}{Character: block name.}

\item{block}{A \code{\link{hank_het2_block}}.}

\item{inputs}{Character: aggregate inputs, a subset of
\code{c("rb", "ra", "w", "Tr")} plus, for a block built with
\code{Pi_fn}/\code{Pi_inputs}, the block's named transition-probability
inputs.  Validated here so a bad wiring fails at spec time rather than
inside the Jacobian dispatch.}

\item{outputs}{Character: a subset of \code{c("B", "A", "C", "CHI")}
(aggregate liquid, illiquid, consumption, adjustment cost).  \code{CHI} is
a real resource cost, so a GE resource constraint that nets it out must
request it.}
}
\value{
An object of class \code{hank_block} (kind \code{"het2"}).
}
\description{
The two-asset counterpart of \code{\link{hank_het_block_spec}}.
}
\seealso{
\code{\link{hank_het_block_spec}} (one-asset),
  \code{\link{hank_het2_block}}, \code{\link{hank_twoasset_model}}
}
