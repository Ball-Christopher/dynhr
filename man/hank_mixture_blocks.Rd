% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-reweighting.R
\name{hank_mixture_blocks}
\alias{hank_mixture_blocks}
\title{Build K heterogeneous-preference het blocks sharing one grid}
\usage{
hank_mixture_blocks(a_grid, Pi, e, betas, eis = 1, r, w)
}
\arguments{
\item{a_grid}{Numeric asset grid (see \code{\link{hank_asset_grid}}),
shared by every type.}

\item{Pi}{Numeric \code{n_e x n_e} income transition matrix, shared.}

\item{e}{Numeric length-\code{n_e} income levels, shared.}

\item{betas}{Numeric length-K vector of discount factors, one per type.}

\item{eis}{Elasticity of intertemporal substitution, shared (scalar; a
single type-invariant value, since only \code{beta} varies here).}

\item{r, w}{Steady-state real return and wage (types interact only through
these prices, so both are common across types).}
}
\value{
A list of length-K \code{\link{hank_het_block}} objects, one per
  entry of \code{betas}, in the same order.
}
\description{
A K-type discount-factor mixture: every type shares the same asset grid,
income process and EIS -- only \code{beta} differs across types (the
object of interest for the het-preferences research line). Because types
never interact directly (only via aggregate prices), each block is an
ordinary \code{\link{hank_het_block}} solved independently at the SAME
\code{(r, w)}.
}
