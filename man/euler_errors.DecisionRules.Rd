% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global-solution-methods.R
\name{euler_errors.DecisionRules}
\alias{euler_errors.DecisionRules}
\title{Model-agnostic Euler-equation accuracy for a perturbation decision rule}
\usage{
\method{euler_errors}{DecisionRules}(
  x,
  compiled,
  params = NULL,
  model = NULL,
  n_grid = 10L,
  n_quad = 20L,
  state_domain = NULL,
  n_sd = 2,
  normalize_by = "auto",
  ...
)
}
\arguments{
\item{x}{A \code{DecisionRules}, \code{DecisionRules2} or
\code{DecisionRules3} object.}

\item{compiled}{The \code{dynhr_compiled} the rule was solved from.}

\item{params}{Named numeric parameter vector (defaults to
\code{compiled$model$param_values}).}

\item{model}{The \code{dynhr_mod} (defaults to \code{compiled$model});
used only for the shock standard deviations.}

\item{n_grid}{Test points per state dimension (default 10).}

\item{n_quad}{Gauss-Hermite nodes per shock dimension (default 20).}

\item{state_domain}{Named list of \code{c(lo, hi)} per state.  \code{NULL}
(default) uses steady state \eqn{\pm} \code{n_sd} unconditional standard
deviations from the first-order stationary covariance.}

\item{n_sd}{Half-width of the default box in state standard deviations
(default 2).}

\item{normalize_by}{Numeraire variable; see
\code{\link{euler_errors.GlobalSolution}}.}

\item{...}{Ignored.}
}
\value{
The same list \code{\link{euler_errors.GlobalSolution}} returns.
}
\description{
The perturbation counterpart of \code{\link{euler_errors.GlobalSolution}}:
the Taylor policy is evaluated UNPRUNED (the standard accuracy convention)
at each grid point and the model's own residuals are integrated over the
\eqn{t+1} shocks by Gauss-Hermite quadrature.  Dispatches for
\code{DecisionRules}, \code{DecisionRules2} and \code{DecisionRules3}, so
the order-1 / order-2 / order-3 accuracy ordering is a single call each.
}
