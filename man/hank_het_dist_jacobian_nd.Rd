% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-jacobian.R
\name{hank_het_dist_jacobian_nd}
\alias{hank_het_dist_jacobian_nd}
\title{Brute-force numerical-differentiation DISTRIBUTION Jacobian of a het block}
\usage{
hank_het_dist_jacobian_nd(block, T_h, inputs = c("r", "w"), delta = 1e-05)
}
\arguments{
\item{block}{A \code{\link{hank_het_block}}.}

\item{T_h}{Integer horizon.}

\item{inputs}{Character subset of \code{c("r", "w")}.}

\item{delta}{Numeric FD step for the input perturbation.}
}
\value{
Named list \code{JD_nd[[input]]}, each a 3-D array of dimension
  \code{T_h x T_h x (n_e*n_a)} with \code{JD_nd[[i]][t, s, ] =
  dD_t/dI_s} (central difference).
}
\description{
Reference sequence-space distribution Jacobian \eqn{J^D[t,s,] = dD_t/dI_s}
(the change in the full \code{(n_e*n_a)}-vector cross-sectional distribution
at date \code{t} induced by a shock to aggregate input \code{i} at date
\code{s}), computed exactly like \code{\link{hank_het_jacobian_nd}} but
keeping the full \code{Dpath} instead of aggregating it into \code{A}/\code{C}.
Used to validate \code{\link{hank_het_dist_jacobian}}.
}
