% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-grids.R
\name{hank_asset_grid}
\alias{hank_asset_grid}
\title{Non-uniform asset grid (log-spaced, clustered near the borrowing constraint)}
\usage{
hank_asset_grid(amax, n, amin = 0, pivot = abs(amin) + 0.25)
}
\arguments{
\item{amax}{Numeric: upper bound of the asset grid.}

\item{n}{Integer >= 2: number of gridpoints.}

\item{amin}{Numeric: borrowing constraint / lower bound (default 0).}

\item{pivot}{Numeric > 0: curvature shift; smaller = more clustering near
\code{amin}.  Default \code{abs(amin) + 0.25} matches sequence-jacobian.}
}
\value{
A strictly increasing numeric vector of length \code{n}, with
  \code{[1] == amin} and \code{[n] == amax}.
}
\description{
Builds an \code{n}-point grid on \eqn{[a_{min}, a_{max}]} that is equidistant
in the logs of \eqn{a + \text{pivot}}, so gridpoints cluster near
\eqn{a_{min}}.  The first point is set exactly to \code{amin}.
}
\examples{
a <- hank_asset_grid(amax = 200, n = 500, amin = 0)
a[1]      # 0
length(a) # 500
}
