% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stochsimul-monolith.R
\name{simulate_model}
\alias{simulate_model}
\title{Simulate the model forward from steady state}
\usage{
simulate_model(
  dr,
  n_periods = 200L,
  shocks = NULL,
  n_replications = 1L,
  model = NULL,
  burn_in = 100L,
  init_state = NULL
)
}
\arguments{
\item{dr}{DecisionRules object}

\item{n_periods}{Number of simulation periods}

\item{shocks}{Matrix of shocks (n_periods x n_exo). If NULL, draws random.}

\item{n_replications}{Number of replications for stochastic simulation.
When \code{1} (default) a plain \code{n_periods x n_endo} matrix is
returned (backward-compatible).  When \code{> 1} a 3-D array of dimension
\code{(n_periods, n_endo, n_replications)} is returned; each slice
\code{[,,r]} is one independent replication drawn with a different random
seed.  The pre-existing \code{attr(., "levels")} attribute is attached to
the first slice only (2-D case) or omitted (3-D case).}

\item{model}{dynhr_mod (for shock variances)}

\item{burn_in}{Burn-in periods to discard}

\item{init_state}{Optional named numeric vector of initial state deviations
(over endogenous names) loaded into the period-1 state; pair with
\code{burn_in = 0}. \code{NULL} starts at the steady state.}
}
\value{
Matrix (n_periods x n_endo) when \code{n_replications = 1};
  3-D array (n_periods x n_endo x n_replications) when \code{> 1}.
}
\description{
Simulate the model forward from steady state
}
