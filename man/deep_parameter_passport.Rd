% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag-deep-passport.R
\name{deep_parameter_passport}
\alias{deep_parameter_passport}
\title{Deep-Parameter Passport}
\usage{
deep_parameter_passport(
  deep_spec,
  suite = NULL,
  draws = NULL,
  axes = NULL,
  meta = NULL
)
}
\arguments{
\item{deep_spec}{A \code{\link{build_deep_spec}} object, or a parsed model
(from which a taxonomy is built).}

\item{suite}{Optional \code{dynhr_diagnostic_suite} (the output of
\code{run_all_diagnostics()}); axes are read from it where present.}

\item{draws}{Optional posterior draw matrix (\eqn{n \times p}) with
named columns; adds posterior median/sd context columns.}

\item{axes}{Optional named list of per-parameter logical vectors that
override the suite-derived axes, e.g.
\code{list(structural = c(theta_H = FALSE, alpha = TRUE))}.}

\item{meta}{Optional \code{\link{diag_meta}} provenance descriptor.}
}
\value{
A \code{dynhr_diagnostic} whose \code{$result$passport} is the tidy
  scorecard \code{data.frame}.
}
\description{
Synthesises a per-parameter "deepness" scorecard from the deep-parameter
taxonomy and the available identification / deep diagnostics.  Each deep
parameter is scored on five axes — identified, informed, structural,
invariant, robust — and given an A-F grade; auxiliary shock parameters are
listed but graded \code{"-"}.
}
