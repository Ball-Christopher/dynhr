% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-aggregate-risk.R
\name{simulate.hank_ks_agg_risk}
\alias{simulate.hank_ks_agg_risk}
\title{Simulate aggregate paths from a Krusell-Smith aggregate-risk solution}
\usage{
\method{simulate}{hank_ks_agg_risk}(
  object,
  nsim = 1,
  seed = NULL,
  T_sim = 200L,
  z_path = NULL,
  D0 = NULL,
  K0 = NULL,
  burn_in = 0L,
  ...
)
}
\arguments{
\item{object}{A \code{hank_ks_agg_risk} object from
\code{\link{hank_ks_aggregate_risk}}.}

\item{nsim}{Integer: number of independent simulated paths.}

\item{seed}{Integer or \code{NULL}: seed for the drawn aggregate paths (and
restored on exit, per the \code{stats::simulate} contract).}

\item{T_sim}{Integer: length of each simulated path.}

\item{z_path}{Optional integer vector of aggregate-state indices to use
instead of drawing one. When supplied, \code{nsim} must be 1 and
\code{T_sim} is taken from its length.}

\item{D0}{Optional initial cross-sectional distribution (length
\code{n_e*n_a}, distribution order). Defaults to the solution's ergodic
MEAN distribution, so a path starts from the stochastic steady state
rather than from the deterministic one.}

\item{K0}{Optional aggregate capital entering the first period; defaults to
the ergodic mean of \code{K}.}

\item{burn_in}{Integer: leading periods dropped from the returned paths
(0 by default -- the default \code{D0} is already ergodic).}

\item{...}{Unused.}
}
\value{
A data frame (\code{nsim = 1}) or a list of data frames, with
  columns \code{t}, \code{Z}, \code{z_index}, \code{K} (capital ENTERING
  the period), \code{K_next}, \code{r}, \code{w}, \code{C}, \code{Y}.
}
\description{
Runs the non-stochastic histogram simulation forward under a supplied or
drawn aggregate-shock path, returning the aggregate series the solution
implies.
}
\seealso{
\code{\link{hank_ks_aggregate_risk}}, \code{\link{hank_ks_risk_irf}}
}
