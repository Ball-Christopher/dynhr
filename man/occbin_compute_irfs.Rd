% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occbin-nonlinear-solve.R
\name{occbin_compute_irfs}
\alias{occbin_compute_irfs}
\title{Compute IRFs using the OccBin path solver (nonlinear or piecewise-linear)}
\usage{
occbin_compute_irfs(
  compiled,
  y_ss,
  model,
  params = NULL,
  constraints = NULL,
  n_periods = 40L,
  shock_size = 1,
  method = c("nonlinear", "pwlinear"),
  ...
)
}
\arguments{
\item{compiled}{dynhr_compiled}

\item{y_ss}{Named numeric vector: steady state}

\item{model}{dynhr_mod (for shock standard deviations)}

\item{params}{Named numeric parameter vector; NULL -> model$param_values}

\item{constraints}{Constraint list from occbin_parse_bind_relax(); NULL ->
auto-resolved from compiled$occbin$parse_result$constraints when available}

\item{n_periods}{Number of IRF periods (default 40L)}

\item{shock_size}{Shock size multiplier (default 1)}

\item{method}{Character: "nonlinear" (default) or "pwlinear".
"pwlinear" uses Dynare's piecewise-linear approach for exact Dynare parity.
"nonlinear" uses the stacked Newton solver for higher accuracy.}

\item{...}{Additional arguments passed to occbin_solve_path()}
}
\value{
IRFCollection object (same structure as compute_irfs()); each shock's
  entry is the DEVIATION from steady state (T x n_endo).  The object also
  carries \code{attr(., "regime_paths")} (a named list, one integer vector
  per shock) and \code{attr(., "converged")} (a named logical vector).
}
\description{
Wraps occbin_solve_path() into the standard IRFCollection interface, similar
to compute_irfs_obc() but using the regime-aware solver.  Supports both the
nonlinear stacked-Newton solver (default, more accurate) and the
piecewise-linear Dynare-style solver (method="pwlinear", exact Dynare parity).
}
