% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-manifest-common.R
\name{.hank_manifest_build}
\alias{.hank_manifest_build}
\title{Assemble a run manifest from an already-classified household object}
\usage{
.hank_manifest_build(
  x,
  is_block,
  peak_rss,
  sizes,
  state_count,
  grid_mass,
  agg_fields,
  class
)
}
\arguments{
\item{x}{The household object (a block, or a bare solve), already
validated and unwrapped by the caller.}

\item{is_block}{Logical: whether \code{x} carries a stationary distribution
and block-level aggregates. Bare solves report the block-only fields as
\code{NA} rather than erroring.}

\item{peak_rss}{Logical: when \code{FALSE}, skip the
\code{\link{.hank_peak_rss}} call and report \code{NA_real_}.}

\item{sizes}{Named list of the family's grid sizes, in reporting order
(e.g. \code{list(n_e =, n_d =, n_f =, n_a =)}).}

\item{state_count}{Integer total cell count.}

\item{grid_mass}{The family's grid-mass vector, or \code{NULL} when no
distribution is available.}

\item{agg_fields}{Character vector of the family's aggregate field names,
read off \code{x} when \code{is_block} and \code{NA_real_} otherwise.}

\item{class}{Character: the S3 class to stamp on the result.}
}
\value{
A named list of class \code{class}.
}
\description{
The shared body of \code{\link{hank_het_manifest}},
\code{\link{hank_het2_manifest}} and \code{\link{hank_het3_manifest}}: it
reads the fields every family carries under the same names (backend,
threads, iterations, converged, gaps, timings, \code{Pi_inputs}) and splices
in the per-family pieces its caller has already computed.
}
\details{
Field ORDER is fixed here and is part of the contract -- it is the column
order of \code{as.data.frame()} on the result.
}
\keyword{internal}
