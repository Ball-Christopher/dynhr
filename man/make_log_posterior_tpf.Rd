% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tpf-likelihood.R
\name{make_log_posterior_tpf}
\alias{make_log_posterior_tpf}
\title{Construct the tempered particle filter log-posterior function}
\usage{
make_log_posterior_tpf(
  model,
  data,
  prior_spec,
  obs_vars,
  compiled,
  me_variance,
  n_particles = 1000L,
  ess_target = 0.5,
  n_mh = 1L,
  mh_scale = 1,
  seed = NULL,
  system_priors = NULL,
  max_stages_u = 16L
)
}
\arguments{
\item{model}{dynhr_mod from \code{\link{parse_mod}}.}

\item{data}{Observation matrix (n_obs x T); columns are time periods.}

\item{prior_spec}{Prior specification from \code{extract_prior_spec}.}

\item{obs_vars}{Character vector of observed variable names.}

\item{compiled}{dynhr_compiled from \code{\link{compile_model}}.}

\item{me_variance}{Measurement error variance (scalar, must be > 0).
This is the TPF tempering instrument; me_variance = 0 is not allowed.}

\item{n_particles}{Number of particles (default 1000).}

\item{ess_target}{ESS resampling threshold as fraction of N (default 0.5).}

\item{n_mh}{RWMH mutation steps per phi stage (default 1).}

\item{mh_scale}{RWMH proposal scale relative to particle cloud covariance.}

\item{seed}{Integer RNG seed for reproducibility (NULL = no fixed seed).}

\item{system_priors}{Optional system priors list (see \code{\link{sp_irf}}).}

\item{max_stages_u}{Maximum number of tempering stages per observation
(default 16).  The filter raises phi from 0 to 1 in at most this many
steps; a smaller value speeds up the filter at the cost of coarser
tempering.}
}
\value{
A \code{function(theta)} returning
  \code{list(logpost, loglik, logprior)}.
}
\description{
Returns a \code{function(theta)} that evaluates the log-posterior using
the Herbst & Schorfheide (2019) Tempered Particle Filter on the pruned
order-2 state space.
}
