% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hank-mixture-emulator.R
\name{hank_emulator_level_metric}
\alias{hank_emulator_level_metric}
\title{Emulated `K x K` covariance of a projected stationary (LEVEL) wealth survey}
\usage{
hank_emulator_level_metric(emu, theta, N)
}
\arguments{
\item{emu}{A \code{\link{hank_mixture_emulator}}.}

\item{theta}{Named list or length-`d` named numeric in `names(emu$box)`
order.}

\item{N}{Sample size of the stationary cross-section.}
}
\value{
A `emu$K x emu$K` numeric covariance matrix.
}
\description{
The covariance of `t(B_ref) Dhat` for a SINGLE size-`N` multinomial
cross-section `Dhat` drawn from the stationary distribution `D0(theta)`,
rebuilt from the interpolated projected pieces `m1, M2D0`:
\deqn{\Sigma_L = cov_{D0}(B\_ref) / N = (M2D0 - m1\, m1^T) / N.}
This is the LEVEL-observable analogue of \code{\link{hank_emulator_metric}}
(which is the RESPONSE observable's covariance of a DIFFERENCE of two
cross-sections); the level uses only one survey, hence the single
`cov_D0` term.
}
\seealso{
\code{\link{hank_emulator_level_mean}}, \code{\link{hank_emulator_metric}}
}
